---
title: "Hypothesis Testing"
interactive-scripts:
  - normal-comparison-tutorials
  - distribution-generator
  - critical-table-generator
  - make-inferential-diagram
---

{{< interactive-figure hypothesisTestingCriticalRegions makeInferenceCover options='{"color":"var(--sfs-null-color, currentColor)","distribution":"normal","style":"minimal","xDomain":[-4,4],"shade":{"tail":"two","alpha":0.05},"animate":true,"ariaLabel":"Normal null distribution with two red critical tails."}' fallback="Normal curve with two-tailed critical regions shaded." >}}

{{< include _coming-soon.qmd >}}

:::::::: {.content-visible when-profile="draft"}

We'll start this chapter by revisiting the idea of radioactive spider-bites affecting reaction times. This time, though, we'll move beyond one unusually quick kid and consider a whole sample of Spider-People. That will present a problem that we'll solve using the tools we've picked up over the last few chapters: $z$-scores, probability, sampling distributions, and the Central Limit Theorem. By putting this all together, we'll complete our first hypothesis test. This will introduce us to some new ideas and terminology, including null and alternative hypotheses, critical regions, and $p$-values. We'll also introduce a formal four-step procedure for hypothesis testing that we'll use throughout the rest of the course.

## Spider-Man revisited

Back in Chapter 5 we considered the possibility that a radioactive spider bite might have caused a difference in the reaction times of one individual. We supposed that following the bite, Peter Parker had a reaction time of 189 milliseconds on a test for which the general population had a mean of 284 milliseconds and a standard deviation of 50 milliseconds. We calculated his $z$-score to be $-1.9$. Assuming reaction times form a normal distribution, the probability of seeing a score this fast or faster is less than three percent.

In other words, we asked what we would expect if the spider bite had done nothing and Peter's reaction time still came from the population of unbitten kids. We would only see a score this fast or faster about three times out of a hundred. That didn't prove that the spider bite had changed his reaction time. But his score was unusual under the no-change explanation, which gave us some reason to doubt it.

That calculation already contained the basic logic of a hypothesis test: if the spider bite had done nothing, what were the chances of seeing a reaction time this fast or faster?

Still, it isn't entirely obvious that being bitten by a radioactive spider ought to *improve* your reaction time. It might slow you down, or merely give you a nasty bite. For now, let's remain open to a change in either direction. We'll return to why that matters when we distinguish one-tailed and two-tailed tests.

But real research doesn't usually rely on examining just one individual. We usually want a *sample*---a bunch of kids who we treated to radioactive spider bites to see if it would alter their reaction times. Obviously in the real world this experiment probably would not make it past the ethics board, but the Spiderverse™️ provides just such a scenario.

![A sample of my son's toy Spider-People, $n = 3$](resources/figures/3-spidermen.gif){#fig-spidermen}

So say we have a sample of three kids, all bitten by radioactive spiders, and we measure their reaction times. The *sample mean* is 189 milliseconds, the same as Peter Parker's individual score. But now we have a sample of three people, not just one. How does that change our analysis? Is it just as surprising as a single score of 189 ms? More surprising? Less? Surprising *enough* to count as evidence that the population mean changed?

{{< include quizzes/08-hypothesis-testing-spider-sample-reflection.qmd >}}

### The Logic of Hypothesis Testing

The move from one Spider-Man to a sample of Spider-People changes the calculation, but not the logic. For Peter's individual score, we used the population distribution to ask what reaction times we would commonly see if nothing had changed. For our sample mean, we need the sampling distribution to ask what sample means we would commonly see under that same no-change assumption.

So let's make the assumption explicit: suppose the spider bites did nothing. More specifically, suppose the population of bitten kids still has the same mean reaction time as the population of unbitten kids, 284 milliseconds. If that's true, our three Spider-People should behave like a random sample from a population with that mean. We can work out what sorts of sample means that population would commonly produce, then see where our observed mean of 189 falls. This assumption that nothing changed will become our *null hypothesis*, though we'll introduce the terminology more formally in a moment.

What we're doing when we run an experiment, theoretically, is applying some treatment to a sample and using the result to make an inference about the population that treated sample represents. That treated population is hypothetical: we're imagining what would happen if we could give the same treatment to everyone. We can't observe its mean directly, so our sample mean is our clue.

{{< interactive-figure zTestInferentialDiagram makeInferentialDiagram options='{"preset":"z-test"}' fallback="Inferential process diagram for a z test." >}}

But here's the rub. Sample means vary due to sampling error, even when the population mean hasn't changed. So if our treated sample has a different mean, we need to judge whether the difference is more than we would commonly expect from sampling error alone. We do that by imagining the sampling distribution we would get if the treatment had no effect, then asking how common or uncommon our observed sample mean would be in that distribution. It is the same basic question we asked about Peter: what are the chances of seeing a result this extreme if nothing actually happened?

The situation is slightly more complicated now because we're locating a sample mean among other possible sample means rather than locating one person among other individuals. But only slightly. We have all the tools we need.

### Putting It All Together

First, let's cast our minds back to $z$-scores. We can calculate a $z$-score for any given value in a population using the population characteristics $\mu$ and $\sigma$: $z = \frac{X - \mu}{\sigma}$. See @eq-z-score-pop. This $z$-score describes that score's position in the population relative to the mean, in units of standard deviations. For normally distributed scores, we can then use the unit normal table to find how much of the population falls at least that far into the relevant tail. That's how we judged Peter's score against what we would expect if the bite had done nothing.

Next, we learned that we can think about samples rather than individual scores by looking at a sampling distribution of the mean. This is a special kind of distribution consisting of every possible sample mean that a population can produce for a given sample size, like our samples of size $n = 2$ from the population of four people of different heights. It lets us do for a sample mean what the population distribution let us do for Peter's individual score: judge how common or uncommon it would be under our no-change assumption.

And finally, we learned that we don't need to list every possible sample mean. The equations from Chapter 7 tell us the center and standard error of the sampling distribution from the population mean, population standard deviation, and sample size. The Central Limit Theorem tells us about its shape: if the population is normal, the sampling distribution of the mean is normal at any sample size; otherwise, it becomes approximately normal as the sample size increases. There is no magic sample size where that suddenly happens. Because our Spider-People sample contains only three observations, our calculation here relies on the simplifying assumption from Chapter 5 that reaction times follow a normal distribution.

So now we're prepared to work out the probability of observing particular *sample statistics* when we have a population mean to test and know the population standard deviation. We can find the $z$-score for a sample mean using a modified $z$ equation:

$$z = \frac{M - \mu_M}{\sigma_M}$$

This $z$-score tells us the position of that particular sample mean relative to all sample means in the sampling distribution, in units of the standard error. Under our normal model, we can then use the same unit normal table that we used for individual $z$-scores to determine the probability of sample means within a particular range.

Now, since $\mu_M$, the mean of the sampling distribution, is equal to $\mu$, the population mean, you'll often see this equation written using $\mu$ instead of $\mu_M$. The idea is the same.

$$z = \frac{M - \mu}{\sigma_M}$$

### From Individual Scores to Sample Means

Thankfully, armed with the sampling distribution, it's not much of a leap to move from single scores to whole samples. The two calculations ask the same question under the same no-change assumption. They simply locate different things in different distributions.

First, let's put the calculation for Peter Parker's single score in front of us again, for old time's sake. We plug his reaction time of 189 milliseconds into the individual-score equation and get $-1.9$.

$$ z = \frac{X - \mu}{\sigma} = \frac{189 - 284}{50} = -1.9 $$

We can look up $z = 1.9$ in the unit normal table to find the lower-tail probability: $P(z \le -1.9) = .0287$. So if Peter Parker's reaction time really came from the population of unbitten kids, we would see a score this fast or faster about three times out of a hundred. This is the directional question we asked in Chapter 5, not yet a formal two-tailed test.

{{< interactive-figure peterZTable makeCriticalValueTable options='{"distribution":"normal","layout":"areas","title":"Unit normal table (excerpt)","z":{"from":1.86,"to":1.94,"step":0.01},"zDigits":2,"areas":["left","right","mean-to-z"],"selected":{"z":1.9,"area":"right"},"maxWidth":"32rem"}' fallback="Excerpt of the unit normal table for z from 1.86 to 1.94, with the area to the right of z = 1.90, .0287, highlighted." >}}

But now let's calculate the $z$-score based on the idea that the score of $189ms$ came not from one individual, but instead it was the sample mean for a sample of three people all bitten by radioactive spiders.

$$ z = \frac{M - \mu_M}{\sigma_M} = \frac{189 - 284}{\frac{50}{\sqrt{3}}} = -3.29 $$

Now our calculation starts with that sample mean. We subtract the mean of the sampling distribution, which is equal to the population mean under our no-change assumption. The Human Benchmark results page supplied the mean of 284 milliseconds; as we admitted in Chapter 5, we invented the standard deviation of 50 milliseconds for this example. Our numerator is unchanged. It's only the denominator that changes. We divide by the standard error of the mean---the variability of the sampling distribution---rather than the variability of individual scores. The standard error is $\sigma$ over the square root of sample size: 50 over the square root of 3. And now we get a $z$-score of $-3.29$, which is quite a bit lower than it was for Peter Parker alone. That's because a sample mean as far from the population mean as this is less common than an individual score that far away. Sample means vary less than individual scores.

And if we look up that $z$-score in the unit normal table, we get a much smaller lower-tail probability: $P(z \le -3.29) = .0005$.

{{< interactive-figure spidermenZTable makeCriticalValueTable options='{"distribution":"normal","layout":"areas","title":"Unit normal table (excerpt)","z":{"from":3.25,"to":3.33,"step":0.01},"zDigits":2,"areas":["left","right","mean-to-z"],"selected":{"z":3.29,"area":"right"},"maxWidth":"32rem"}' fallback="Excerpt of the unit normal table for z from 3.25 to 3.33, with the area to the right of z = 3.29, .0005, highlighted." >}}

To show this another way, here's what the sampling distribution looks like and where our sample mean lies in that distribution. Here it's shown in the original units; we could relabel the axis with $z$-scores instead. Remember, the $z$-scores are just a change of label. They don't transform the positions or the variability of the scores. So with the original units, our sample mean is still exactly as far down in that extreme tail of the distribution.

{{< interactive-figure spidermenSamplingDistribution makeDistributionGraph options='{"color":"var(--sfs-null-color, currentColor)","distribution":"normal","mean":284,"sd":28.87,"xDomain":[165,375],"xTicks":8,"style":"minimal","xAxis":true,"shade":{"to":189},"markers":{"at":189,"label":"M = 189","height":0.45,"color":"var(--sfs-accent, #2c7fb8)"},"animate":true,"labels":{"x":"Sample mean reaction time (ms), n = 3"}}' fallback="Sampling distribution of mean reaction times for samples of three, centered on 284 ms with standard error 28.87 ms; a vertical line marks the sample mean of 189 ms far down in the lower tail." >}}

In other words, if spider bites leave the population mean at 284 milliseconds, getting a sample of three people with an average of 189 milliseconds is vanishingly unlikely. The observed sample is difficult to reconcile with our starting assumption that the bites did nothing. That doesn't prove what caused the difference, but it gives us evidence against the no-change model.

Or to put it in terms of the inferential process diagram, we're asking whether the treated population could still have the original mean. If it does, our model says that a sample mean this low or lower has a probability of about one in two thousand on any one draw. That makes our observed sample very awkward for the no-change assumption.

## What Is a Hypothesis Test?

We've effectively just completed our first hypothesis test. A hypothesis is just something you suspect might be true about the world. We suspected that bitten people may have different reaction times than unbitten people. But we remained open to the possibility that the bites did nothing. We used that skeptical possibility as a working model and asked what it predicted. This is the *null hypothesis*.

Ronald A. Fisher was a British statistician and geneticist, and one of the central figures in the development of modern statistics. Among many other contributions, he helped develop and popularize significance testing and the use of null hypotheses in experimental research. In his 1935 book *The Design of Experiments*, he put the logic rather neatly:

> “Every experiment may be said to exist only in order to give the facts a chance of disproving the null hypothesis.”
>
> ---R. A. Fisher, *The Design of Experiments* [@fisher1935design, p. 16]

“Disproving” is stronger language than we'll usually use. A statistical result cannot establish with logical certainty that the null hypothesis is false. But Fisher's line captures the direction of the argument. We work out what the null hypothesis predicts, then give the data a chance to contradict it. That doesn't mean we begin by believing that the null hypothesis is true. It gives us a precise model to test. We used our knowledge of sampling distributions and probability to determine what kind of sample means would be common or uncommon if the population mean remained unchanged. Then we compared the $z$-score for our Spider-People's sample mean with that null distribution. The observed result was unusually far out in the tail, so it counted as evidence against the null hypothesis.

## The Four-Step Hypothesis Testing Procedure

To put this procedure more formally, we can think of this as a four-step procedure. We'll talk about each step in more detail since there's some new terminology to introduce along the way, but essentially it's the procedure we just walked through, dressed up in slightly more formal language. We state our hypotheses first. We set our decision criteria. Next, we collect and analyze the data. And then we make a decision.

### Step 1: State the Hypotheses

Step one of the hypothesis testing procedure is to formally state our hypotheses. To give it its full title, this procedure is called Null Hypothesis Significance Testing, or NHST for short. That name refers to the skeptical working model we just used. For our Spider-People, the null hypothesis says that the population mean reaction time remains 284 milliseconds after a bite. The alternative says that the mean changes:

$$H_0: \mu_{\text{bitten}} = 284 \qquad H_1: \mu_{\text{bitten}} \neq 284$$

Notice that the two hypotheses don't play symmetrical roles in this test. The null gives us a specific population mean: 284 milliseconds. Along with our other assumptions, that value defines a specific sampling distribution. It therefore gives us something we can test directly: if the population mean really is 284, how often would we get a sample mean this far from it or farther?

The alternative hypothesis is less specific. It says that the mean is *not* 284, but that could mean 283, 250, 400, or any other value. It doesn't supply one alternative mean from which we could construct one alternative sampling distribution. So, in the significance test we're doing here, it is the null hypothesis that we test directly. The alternative tells us what counts as a departure from it---in this case, a difference in either direction.

That point can be overstated. We *could* specify a particular alternative, such as $\mu_{\text{bitten}} = 250$. That would give us another sampling distribution to work with, and we'll need just such a specific alternative when we discuss statistical power in the next chapter. But our alternative here, $\mu_{\text{bitten}} \neq 284$, does not make one precise numerical prediction.

The null doesn't have to say that two populations are identical in every imaginable respect. It makes a claim about the particular parameter we're testing---the population mean, in this case. In other statistical tests, the null might state that two treatment conditions have equal population means, or that there is no association between two variables. In broad terms, the null says that the particular effect or relationship we're testing is absent.

These two propositions are logical complements: the bitten population mean is either equal to 284 or it isn't. They can't both be true. But that doesn't make the evidence perfectly symmetrical. We're going to ask how well our data fit the null model. A result that would be very unusual under that model gives us reason to reject it, though it doesn't prove any particular alternative or establish what caused the difference. And a result that isn't unusual under the null only means that we fail to reject it; it doesn't prove the null hypothesis true. The test won't reveal either proposition to be true or false with certainty, but it can tell us whether the data provide sufficient evidence to reject the null.

So here are some examples based on some psychological effects we might be interested in. 

Say some pharma company thinks it has invented a pill that makes people smarter. The null hypothesis there would be that the pill has no effect on intelligence. The alternative would be that it does affect intelligence. 

- General research question: Does this intervention affect intelligence? 💊🧠
    - $H_0$: The intervention has no effect on intelligence.
    - $H_1$: The intervention has an effect on intelligence.

Or say some researchers suspect that adopting an expansive body posture--like the Superman pose--makes people feel more confident. The null would be the posture does not affect confidence. The alternative would be that it does.

- General research question: Does this posture affect confidence? 🦸‍♀ 😎️
    - $H_0$: The posture has no effect on confidence.
    - $H_1$: The posture has an effect on confidence.

Or to take one more example, say we're interested in correlations and we think that there will be an association between the number of years of education someone has completed and their lifetime earnings. The null hypothesis would say that there is no association between education and income. The alternative would say that there is.

- General research question: Is there an association between education and income? 🎓🤑
    - $H_0$: There is no association between education and income.
    - $H_1$: There is an association between education and income.

We won't say much more about *formulating* hypotheses for this class. That's an issue for research methods. We'll focus more on what happens next: the statistical analyses used to test hypotheses. But if you really think about it, this should be by far the most important and effortful step of the entire research process. Formulating new, interesting, sensible, plausible, meaningful hypotheses is a demanding process. Even to begin to come up with reasonable and potentially insightful hypotheses requires familiarity with the existing research and theoretical perspectives on the phenomena you're interested in. But that's something you should go into in more detail in other classes as you address research methods and specialized topics within psychology.

{{< include quizzes/08-hypothesis-testing-stating-hypotheses.qmd >}}

### Step 2: Set the Decision Criteria

For step two, we're essentially asking, "if the null hypothesis is true, what sample statistics are we most likely or unlikely to see?" Our statistical model tells us what the null sampling distribution will look like. Exactly how we determine its characteristics depends on the nature of our data and the appropriate test statistic. We'll talk more about that as we go. But in a general sense, we are using our knowledge of probability and sampling to determine what results would be highly unusual under the null hypothesis. If we see one that is sufficiently unusual, we will reject the null.

Here, we need to introduce the terminology of $\alpha$ and $p$-values. The Greek symbol, lowercase $\alpha$, is our decision criterion: a cutoff chosen before we analyze the data. The $p$-value is calculated from the observed data. It is the probability, assuming the null model and its other assumptions are correct, of obtaining a test statistic as extreme as or more extreme than the one we observed. What counts as "extreme" depends on whether our alternative hypothesis is one- or two-tailed.

By convention, psychology often uses an alpha level of .05, placing the most extreme five percent of the null distribution in the rejection region. If our sample's $p$-value is less than $\alpha$, we call the result statistically significant and reject the null hypothesis. So when you see $p < .05$ in a published journal article, this is what it means. Neither number is the probability that the null hypothesis is true. When the null is true and the test's assumptions hold, $\alpha$ is the long-run probability that the procedure will reject it by mistake.

The **critical region** contains the sample statistics that would lead us to reject the null hypothesis. Its boundary is the **critical value**. For an upper-tailed test with $\alpha = .05$, for example, the critical value is the 95th percentile of the null distribution: 95 percent lies below it, and five percent lies above it.

### One-Tailed and Two-Tailed Tests

We don't have to have just one critical region. If we do, it's called a one-tailed test, because our critical region is in one tail of the distribution. But we can have a two-tailed test where we specify critical regions at both extremes. If we do this, we're dividing our chosen $\alpha$ into two. If $\alpha$ is .05, or five percent, then by having a two-tailed hypothesis test, we're dividing that five percent between the two tails of the distribution, giving two and a half percent at each end that will cause us to reject the null hypothesis.

In percentile terms, the two critical values are at the 2.5th and 97.5th percentiles. The middle 95 percent of the null hypothesis distribution lies between them, leaving the most extreme five percent outside them.

Whether we want a one- or two-tailed test depends on how we stated our hypotheses in step one. We talk about directional or non-directional hypotheses. With a one-tailed or directional hypothesis, the researcher specifies in advance that they are looking for either an increase or a decrease. With a two-tailed, non-directional test, we're looking for a difference in either direction. Since we don't really know what radioactive spider bites ought to do to a person's reaction time, our formal Spider-People test will be two-tailed. The main point of step two is that we determine up front what kind of results would lead us to reject the null. We don't choose a convenient tail after seeing the data.

::::: {#act-alpha-critical-regions .callout-tip .interactive .with-controls}
## Explore alpha and critical regions

{{< interactive-figure alphaTutorial makeAlphaTutorial fallback="Explore alpha and critical regions. Use the accompanying steps for an explanation." >}}

:::: {.callout-footer}
::: {.tutorial-step data-title="Two tails" data-action='{"alpha":0.05,"tail":"two","z":-1.8}'}
The curve shows the sampling distribution of $z$ when the null hypothesis is true. With $\alpha = .05$ split between two tails, the critical values are about $-1.96$ and $1.96$. The shaded regions together contain five percent of the area. Our hypothetical sample has $z = -1.8$, so it falls just short of the lower critical region.
:::

::: {.tutorial-step data-title="A stricter cutoff" data-action='{"alpha":0.01,"tail":"two","z":-1.8}'}
Reduce alpha to $.01$. The critical regions shrink, and their boundaries move farther from zero, to about $\pm 2.58$. We now require a more extreme result to reject the null. Notice what hasn't changed: the observed $z$ and its $p$-value. Changing our decision criterion doesn't change the evidence in the sample.
:::

::: {.tutorial-step data-title="A lower tail" data-action='{"alpha":0.05,"tail":"left","z":-1.8}'}
Now compare a plan that put all of $\alpha = .05$ in the lower tail. Its critical value is about $-1.64$, so $z = -1.8$ would lead us to reject the null. A lower-tailed test is relevant when the prespecified alternative predicts a decrease, such as faster reaction times. We're comparing possible plans here; we don't choose the direction after seeing the result.
:::

::: {.tutorial-step data-title="An upper tail" data-action='{"alpha":0.05,"tail":"right","z":-1.8}'}
An upper-tailed plan puts the same five percent at the other end. Our negative $z$ doesn't support that alternative, and its upper-tailed $p$-value is large. The direction determines which results count as evidence against the null. It changes both the critical region and the calculation of $p$.
:::

::: {.tutorial-step data-title="Try it" data-action='{"controls-open":true,"alpha":0.05,"tail":"two","z":-1.8}'}
Try changing alpha while keeping the observed $z$ fixed. Then move $z$ across a critical boundary. Crossing into the shaded region and getting $p < \alpha$ are two ways of reaching the same decision. When the null is true and the test's assumptions hold, alpha is also the long-run probability of rejecting it by mistake. It isn't the probability that this particular conclusion is wrong.
:::

::::
:::::

### Step 3: Collect and Analyze the Data

Step three is to conduct the research, gather data, and calculate the appropriate sample statistics. Again, there are many issues around data collection and research methods that we're going to gloss over here and leave for other classes. For our purposes, recall that the version of the Central Limit Theorem we're using assumes independent observations drawn from the same population distribution. Random sampling is one way to support those assumptions, though in practice social scientists often collect data in less tidy ways.

But in any case, the exact statistics we will calculate depend on the nature of the data. We'll start here with the $z$-test. We'll encounter a few different examples over the rest of the semester, including the $t$-test, analysis of variance (ANOVA), and correlation and regression. All these tests rely on the same fundamental hypothesis testing ideas that we're outlining here.

### Step 4: Make a Decision

And now everything is in place for us to make our decision. If the test statistic falls in the critical region---equivalently, if $p < \alpha$---we reject the null hypothesis. Otherwise, we *fail to reject* it. Failing to reject the null isn't the same as accepting it or showing that nothing happened. It means this study did not provide sufficient evidence against it.

## The $z$ test

So we can now apply this formal hypothesis testing procedure using our first real test statistic. It's called the one-sample $z$-test because it relies on the $z$-score for a sample mean that we introduced at the start of this chapter. We use it to test a hypothesized population mean, which we'll write as $\mu_0$, when the population standard deviation $\sigma$ is treated as known and the sampling distribution of the mean is normal or sufficiently close to normal.

This $z$-test isn't actually used very often because in real research we rarely know the population standard deviation. Usually we have to estimate it from sample data, which is why we'll need the $t$-test introduced later. But the $z$-test is a good place for us to start.

To put it in terms of the diagram again, our null hypothesis supplies a particular population mean, $\mu_0$. We calculate the sampling distribution we would expect if the treatment left the mean at that value. Then we locate our treated sample mean within that null distribution by calculating its $z$-score. The farther it lies into the appropriate tail or tails, the more difficult the result is to explain under the null model.

### The Spider Example: Formal Procedure {#sec-spider-test}

So now let's quickly run through the spider example one more time using the formal step-by-step procedure. We have already stated our hypotheses: $H_0: \mu_{\text{bitten}} = 284$ and $H_1: \mu_{\text{bitten}} \neq 284$. This is a non-directional hypothesis, allowing for the possibility that being bitten by a radioactive spider is actually quite bad for your reaction times and probably for your health in general.

Next, we determine our decision criteria. The $z$-test is appropriate for our simplified example because we're treating 284 and 50 milliseconds as the known mean and standard deviation of the reference population. And we know we'll be dealing with a normally distributed sampling distribution because, back when we first did this reaction time test, we assumed for the sake of argument that the population is normal. That isn't strictly true for reaction times, but let's not worry about it here. We can set $\alpha$ at .05, giving us critical regions of .025, or two and a half percent, at either end of the distribution. The corresponding $z$-score cutoffs in the unit normal table are $\pm 1.96$.

{{< interactive-figure criticalZTable makeCriticalValueTable options='{"distribution":"normal","layout":"areas","title":"Unit normal table (excerpt)","z":{"from":1.92,"to":2.0,"step":0.01},"zDigits":2,"areas":["left","right","mean-to-z"],"selected":{"z":1.96,"area":"right"},"maxWidth":"32rem"}' fallback="Excerpt of the unit normal table for z from 1.92 to 2.00, with the area to the right of z = 1.96, .0250, highlighted." >}}

Next, we collect and analyze our data. We already did this earlier. Plugging in all the values to the $z$-score equation, remembering that our denominator now is the standard error of the mean, we got a $z$-score of $-3.29$.

$$\sigma_M = \frac{50}{\sqrt{3}} = 28.87 \qquad z = \frac{189 - 284}{28.87} = -3.29$$

And since that is within our lower critical region, step four is to reject the null hypothesis. For this two-tailed test, we count results at least this far from the null mean in either direction, giving $p \approx .0010$. Since $p < \alpha$, we declare a statistically significant result. In plain language, the sample would be extraordinarily unusual if the population mean really remained 284 milliseconds. That gives us evidence that the mean changed. It does not, by itself, prove that the spider bites caused the change.

## Practice: CBT for OCD

We'll end by having you perform your own hypothesis test. We're going back to the cognitive behavioral therapy example from Chapter 5, based on a study using the Dimensional Obsessive-Compulsive Scale to measure symptoms before and after treatment [@abramowitz2010docs].

And we're going to play the same slightly dishonest teaching trick again. For the moment, pretend that untreated people with OCD form a known reference population with a mean of 30.25 and a standard deviation of 14.89. A treated sample of 40 people has a mean of 15.49. First articulate the null and alternative hypotheses, then set your decision criteria, calculate the test statistic, and make your decision.


{{< include quizzes/08-hypothesis-testing-learning-check.qmd >}}

I should tell you that, like when I first used this example in Chapter 5, I'm misusing the data here again. As before, the numbers are from the real study, but 30.25 and 14.89 were pre-treatment *sample statistics*, not known population parameters. More importantly, the mean of 15.49 came from the *same 40 people* measured after treatment. That makes this a related-samples design, so this $z$-test is not the analysis we would use for the real study. We'll return to the actual design, and the test it calls for, when we introduce the related-samples $t$-test in Chapter 13.

::::::::
